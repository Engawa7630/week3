## 练习6：重写提交信息

### 原始提交信息 1
> fix bug

**重写后**：
fix: 修复空白姓名输入导致的非预期退出码

当 --name 参数为空白字符串时，程序原本以状态码 0 退出并输出带空格的问候语。
这违反了 CLI 工具关于错误输入应返回非零退出码的约定。

修复方案：增加输入验证，若 name 为空或仅含空白字符，则输出错误到 stderr 并以状态码 2 退出。

### 原始提交信息 2
> add stuff

**重写后**：
feat: 添加用户认证模块

为支持多用户登录，新增 auth 模块：

实现基于 JWT 的 token 生成与验证

添加登录/登出接口

增加用户会话管理

影响范围：新增 auth.py，修改 api/routes.py

### 原始提交信息 3
> update code

**重写后**：
refactor: 优化数据库查询性能

将原来的 N+1 查询问题改为使用 JOIN 批量加载关联数据。
测试环境下响应时间从 2.3s 降低到 0.4s。

备选方案：曾考虑使用缓存，但因数据实时性要求高而放弃。
